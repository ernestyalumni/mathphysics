# HEARTBEAT.md

# Empty = no periodic heartbeat work.
#
# When TMPAI is configured to poll periodically (e.g. via Hermes cron in the `tmpai` profile),
# put a short checklist here. Suggested items:
#
# - Read `TASKS.md` and the active branch's last commit. Any blocker?
# - Check `agent/discoveries/` for unbookmarked successful runs from the last 24h.
# - Check `agent/marketing/drafts/` for unapproved drafts ≥ 7 days old; nudge if any.
# - arXiv watch: skim `hep-th`, `hep-ph`, `math.DG`, `math.AG` new submissions; log 5-line summaries
#   to `agent/arxiv-watch/YYYY-Www.md` (weekly only — daily is noise).
#
# Keep this short to limit token burn.
