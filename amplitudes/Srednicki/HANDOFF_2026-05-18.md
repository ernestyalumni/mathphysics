# Handoff 2026-05-18 — Srednicki Companion Cleanup

## Context

Ernest wants the Srednicki QFT work to move away from chapter-by-chapter generated
artifacts as the durable source of truth. The target is a cleaner
`Srednicki_Companion_Full.tex` that captures the useful material from the individual
chapter files concisely but thoroughly.

The individual chapter files under `individual_chapters/` should be treated as audit
inputs or historical generated artifacts. If a chapter is already fully captured in the
full companion, it is reasonable to remove the corresponding individual chapter files.
If an individual chapter is wrong or does not correspond to Srednicki, audit the Python
source before carrying anything forward.

## Current Branch

- Repo: `/home/propdev/.openclaw/workspace/workspace2/repos/mathphysics`
- Branch: `feat/srednicki-companion-latex`
- Base at branch creation: `master` / `origin/master`
- Cleanup commit already made: `6bce662 fix: clean Srednicki companion master`
- Unrelated untracked file present before this handoff: `amplitudes/MHVamplitudes/README.md`

Do not commit directly to `master`. Ernest merges feature branches manually.

## What Was Done

1. Verified that Chapter 34 is included in the full companion:
   - `Srednicki_Companion_Full.tex` includes Chapter 34 as
     `individual_chapters/ch34_left_right_spinors_clean`.
   - `ch34_left_right_spinors_clean.tex` was compared against
     `ch34_left_right_spinors.tex`; the substantive difference is the standalone wrapper
     removal, not content loss.

2. Cleaned only the full companion assembly path:
   - Removed `fullpage` from `Srednicki_Companion_Full.tex` because it overrides the
     `geometry` headheight setting and caused `fancyhdr` warnings.
   - Set `mhchem` to `\usepackage[version=4]{mhchem}`.
   - Set `hyperref` with `hypertexnames=false` to avoid duplicate-anchor warnings from
     reused Srednicki-style equation tags.
   - Added `\inputstandalonechapter{...}` and used it for Chapter 34 so the full companion
     can suppress standalone chapter boilerplate like `\tableofcontents` and `\newpage`
     without editing the generated individual chapter file.

3. Updated `AGENTS.md` with the local caveat:
   - Individual chapter PDFs/build outputs should not be committed.
   - `Srednicki_Companion_Full.pdf` is allowed to be committed when
     `Srednicki_Companion_Full.tex` changes.
   - Full companion LaTeX intermediates (`.aux`, `.log`, `.out`, `.toc`, `.fls`,
     `.fdb_latexmk`) should not be committed.

4. Recompiled the full companion:
   - Command run from `amplitudes/Srednicki/`:
     `pdflatex -interaction=nonstopmode -halt-on-error Srednicki_Companion_Full.tex`
     twice.
   - Output: `Srednicki_Companion_Full.pdf`, 100 pages.
   - Fatal LaTeX errors: none.
   - The old `headheight`, `mhchem`, duplicate-anchor, and stale-rerun warnings were cleared.
   - Remaining warnings are layout/bookmark polish only: overfull boxes, math in PDF
     strings, and a couple `mdframed` page-break warnings.

## Important Clarification From Ernest

When Ernest says "clean", he means clean `Srednicki_Companion_Full.tex`, not clean up
`ch34_left_right_spinors_clean.tex` or other individual chapter files.

Do not spend effort polishing individual chapter artifacts if the plan is to delete them.
Use them to audit coverage, then either:

- remove them once the full companion captures the material, or
- audit/fix the underlying Python if the individual chapter is wrong or not actually
  Srednicki content.

## Suggested Next Steps

1. Let Ernest manually remove the Chapter 34 individual chapter files first, as he offered,
   then inspect the diff to learn the deletion pattern he wants.
2. For each remaining chapter:
   - compare the individual `.tex`/PDF against `Srednicki_Companion_Full.tex`;
   - decide whether the full companion already captures the useful material;
   - if yes, remove the individual generated artifacts and keep/update the full companion;
   - if no, port the missing material into the full companion first.
3. Keep the chapter Python scripts only if they provide reusable computations or tests.
   Long term, move reusable QFT logic into common Python modules instead of one-off
   chapter scripts.
4. Treat chapter-specific equations/results as candidates for unit or integration tests,
   with the corresponding Srednicki chapter as the reference.

## Verification Commands For Next Agent

From `amplitudes/Srednicki/`:

```bash
pdflatex -interaction=nonstopmode -halt-on-error Srednicki_Companion_Full.tex
pdflatex -interaction=nonstopmode -halt-on-error Srednicki_Companion_Full.tex
rg -n "(^!|LaTeX Error|Emergency stop|Fatal error|headheight|mhchem Warning|destination with the same identifier|referenced but does not exist|Rerun to get|Label\\(s\\) may have changed)" Srednicki_Companion_Full.log
```

The final `rg` should return no substantive compile-health findings. It may still find
normal assignment/log internals if the pattern is broadened; inspect context before acting.
